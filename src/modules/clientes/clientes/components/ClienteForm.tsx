/**\n * Componente ClienteForm\n * Formulario para crear y editar clientes\n */\n\nimport { useCliente } from '../hooks/useCliente';\nimport { FormInput } from '@/shared/components/FormInput';\nimport { FormSelect } from '@/shared/components/FormSelect';\n\ninterface ClienteFormProps {\n  onSuccess?: () => void;\n}\n\nconst TIPOS_DOCUMENTO = [\n  { value: 'CC', label: 'Cédula de Ciudadanía' },\n  { value: 'CE', label: 'Cédula de Extranjería' },\n  { value: 'PA', label: 'Pasaporte' },\n  { value: 'PP', label: 'Permiso Permanente' },\n];\n\nexport function ClienteForm({ onSuccess }: ClienteFormProps) {\n  const { form, onSubmit, isLoading, success, error } = useCliente();\n\n  const handleSubmit = async (data: any) => {\n    await onSubmit(data);\n    if (success) {\n      onSuccess?.();\n    }\n  };\n\n  return (\n    <div className=\"max-w-4xl mx-auto p-6 bg-slate-900 rounded-xl shadow-lg border border-slate-800\">\n      <h1 className=\"text-3xl font-bold text-white mb-2\">Registrar Cliente</h1>\n      <p className=\"text-slate-400 mb-8\">Completa los datos del cliente para futuras operaciones</p>\n\n      <form onSubmit={form.handleSubmit(handleSubmit)} className=\"space-y-6\">\n        {/* Documento */}\n        <div className=\"grid grid-cols-1 md:grid-cols-2 gap-6\">\n          <FormSelect\n            label=\"Tipo de Documento\"\n            placeholder=\"Selecciona tipo de documento\"\n            options={TIPOS_DOCUMENTO}\n            {...form.register('tipoDocumento')}\n            error={form.formState.errors.tipoDocumento}\n          />\n\n          <FormInput\n            label=\"Número de Documento\"\n            type=\"text\"\n            placeholder=\"123456789\"\n            {...form.register('numeroDocumento')}\n            error={form.formState.errors.numeroDocumento}\n          />\n        </div>\n\n        {/* Nombre y Apellido */}\n        <div className=\"grid grid-cols-1 md:grid-cols-2 gap-6\">\n          <FormInput\n            label=\"Nombre\"\n            type=\"text\"\n            placeholder=\"Juan\"\n            {...form.register('nombre')}\n            error={form.formState.errors.nombre}\n          />\n\n          <FormInput\n            label=\"Apellido\"\n            type=\"text\"\n            placeholder=\"Pérez\"\n            {...form.register('apellido')}\n            error={form.formState.errors.apellido}\n          />\n        </div>\n\n        {/* Contacto */}\n        <div className=\"grid grid-cols-1 md:grid-cols-2 gap-6\">\n          <FormInput\n            label=\"Email (Opcional)\"\n            type=\"email\"\n            placeholder=\"juan@example.com\"\n            {...form.register('email')}\n            error={form.formState.errors.email}\n          />\n\n          <FormInput\n            label=\"Teléfono (Opcional)\"\n            type=\"tel\"\n            placeholder=\"3001234567\"\n            {...form.register('telefono')}\n            error={form.formState.errors.telefono}\n          />\n        </div>\n\n        {/* Dirección y Ciudad */}\n        <div className=\"grid grid-cols-1 md:grid-cols-2 gap-6\">\n          <FormInput\n            label=\"Dirección (Opcional)\"\n            type=\"text\"\n            placeholder=\"Calle 123 #45-67\"\n            {...form.register('direccion')}\n            error={form.formState.errors.direccion}\n          />\n\n          <FormInput\n            label=\"Ciudad (Opcional)\"\n            type=\"text\"\n            placeholder=\"Bogotá\"\n            {...form.register('ciudad')}\n            error={form.formState.errors.ciudad}\n          />\n        </div>\n\n        {/* Errores */}\n        {error && (\n          <div className=\"p-4 bg-red-900/30 border border-red-700 rounded-lg text-red-200 text-sm\">\n            {error}\n          </div>\n        )}\n\n        {/* Éxito */}\n        {success && (\n          <div className=\"p-4 bg-emerald-900/30 border border-emerald-700 rounded-lg text-emerald-200 text-sm\">\n            ✓ Cliente guardado exitosamente\n          </div>\n        )}\n\n        {/* Botón Submit */}\n        <button\n          type=\"submit\"\n          disabled={isLoading}\n          className=\"w-full py-3 bg-emerald-600 hover:bg-emerald-700 disabled:opacity-60 disabled:cursor-not-allowed text-white font-semibold rounded-lg transition duration-200\"\n        >\n          {isLoading ? 'Guardando...' : 'Guardar Cliente'}\n        </button>\n      </form>\n    </div>\n  );\n}\n