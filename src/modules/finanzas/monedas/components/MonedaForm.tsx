/**\n * Componente MonedaForm\n * Formulario para crear y editar monedas\n */\n\nimport { useMoneda } from '../hooks/useMoneda';\nimport { FormInput } from '@/shared/components/FormInput';\n\ninterface MonedaFormProps {\n  onSuccess?: () => void;\n}\n\nexport function MonedaForm({ onSuccess }: MonedaFormProps) {\n  const { form, onSubmit, isLoading, success, error } = useMoneda();\n\n  const handleSubmit = async (data: any) => {\n    await onSubmit(data);\n    if (success) {\n      onSuccess?.();\n    }\n  };\n\n  return (\n    <div className=\"max-w-2xl mx-auto p-6 bg-slate-900 rounded-xl shadow-lg border border-slate-800\">\n      <h1 className=\"text-3xl font-bold text-white mb-2\">Crear/Editar Moneda</h1>\n      <p className=\"text-slate-400 mb-8\">Registra una nueva moneda en el sistema</p>\n\n      <form onSubmit={form.handleSubmit(handleSubmit)} className=\"space-y-6\">\n        {/* Nombre */}\n        <FormInput\n          label=\"Nombre\"\n          type=\"text\"\n          placeholder=\"Ej: Dólar Estadounidense\"\n          {...form.register('nombre')}\n          error={form.formState.errors.nombre}\n          helperText=\"Nombre descriptivo de la moneda\"\n        />\n\n        {/* Código ISO y Símbolo */}\n        <div className=\"grid grid-cols-1 md:grid-cols-2 gap-6\">\n          <FormInput\n            label=\"Código ISO (3 caracteres)\"\n            type=\"text\"\n            placeholder=\"USD\"\n            maxLength={3}\n            {...form.register('codigo')}\n            error={form.formState.errors.codigo}\n            helperText=\"Ej: USD, EUR, MXN\"\n          />\n\n          <FormInput\n            label=\"Símbolo\"\n            type=\"text\"\n            placeholder=\"$\"\n            maxLength={5}\n            {...form.register('simbolo')}\n            error={form.formState.errors.simbolo}\n            helperText=\"Ej: $, €, ¥\"\n          />\n        </div>\n\n        {/* Activa */}\n        <div className=\"flex items-center gap-3 p-4 bg-slate-800 rounded-lg border border-slate-700\">\n          <input\n            type=\"checkbox\"\n            id=\"activa\"\n            className=\"w-5 h-5 rounded border-slate-700 bg-slate-800 text-emerald-600 focus:ring-emerald-500 cursor-pointer\"\n            {...form.register('activa')}\n          />\n          <label htmlFor=\"activa\" className=\"text-slate-300 font-medium cursor-pointer\">\n            Moneda activa (disponible para operaciones)\n          </label>\n        </div>\n\n        {/* Errores */}\n        {error && (\n          <div className=\"p-4 bg-red-900/30 border border-red-700 rounded-lg text-red-200 text-sm\">\n            {error}\n          </div>\n        )}\n\n        {/* Éxito */}\n        {success && (\n          <div className=\"p-4 bg-emerald-900/30 border border-emerald-700 rounded-lg text-emerald-200 text-sm\">\n            ✓ Moneda guardada exitosamente\n          </div>\n        )}\n\n        {/* Botón Submit */}\n        <button\n          type=\"submit\"\n          disabled={isLoading}\n          className=\"w-full py-3 bg-emerald-600 hover:bg-emerald-700 disabled:opacity-60 disabled:cursor-not-allowed text-white font-semibold rounded-lg transition duration-200\"\n        >\n          {isLoading ? 'Guardando...' : 'Guardar Moneda'}\n        </button>\n      </form>\n    </div>\n  );\n}\n