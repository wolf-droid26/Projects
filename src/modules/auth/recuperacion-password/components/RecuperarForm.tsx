/**\n * Componente RecuperarForm\n * Formulario para recuperación de contraseña\n */\n\nimport { useRecuperarPassword } from '../hooks/useRecuperarPassword';\nimport { FormInput } from '@/shared/components/FormInput';\nimport { useNavigate } from 'react-router-dom';\n\ninterface RecuperarFormProps {\n  onSuccess?: () => void;\n}\n\nexport function RecuperarForm({ onSuccess }: RecuperarFormProps) {\n  const navigate = useNavigate();\n  const { form, onSubmit, enviarCodigo, step, isLoading, success, error } = useRecuperarPassword();\n\n  const handleEnviarCodigo = async () => {\n    const email = form.getValues('email');\n    if (email) {\n      await enviarCodigo(email);\n    }\n  };\n\n  const handleSubmit = async (data: any) => {\n    await onSubmit(data);\n    if (success) {\n      onSuccess?.() || navigate('/login');\n    }\n  };\n\n  return (\n    <div className=\"min-h-screen flex items-center justify-center bg-slate-950 px-4\">\n      <div className=\"w-full max-w-md p-8 bg-slate-900 rounded-xl shadow-2xl border border-slate-800\">\n        <h1 className=\"text-3xl font-bold text-white mb-2 text-center\">Recuperar Contraseña</h1>\n        <p className=\"text-sm text-slate-400 text-center mb-8\">Ingresa tu email para recibir un código de verificación</p>\n\n        <form onSubmit={form.handleSubmit(handleSubmit)} className=\"space-y-6\">\n          {/* Paso 1: Email */}\n          {step === 'email' && (\n            <>\n              <FormInput\n                label=\"Email\"\n                type=\"email\"\n                placeholder=\"tu@email.com\"\n                disabled={isLoading}\n                {...form.register('email')}\n                error={form.formState.errors.email}\n              />\n\n              {error && (\n                <div className=\"p-4 bg-red-900/30 border border-red-700 rounded-lg text-red-200 text-sm\">\n                  {error}\n                </div>\n              )}\n\n              <button\n                type=\"button\"\n                onClick={handleEnviarCodigo}\n                disabled={isLoading}\n                className=\"w-full py-3 bg-emerald-600 hover:bg-emerald-700 disabled:opacity-60 disabled:cursor-not-allowed text-white font-semibold rounded-lg transition duration-200\"\n              >\n                {isLoading ? 'Enviando código...' : 'Enviar código'}\n              </button>\n            </>\n          )}\n\n          {/* Paso 2: Código y Nueva Contraseña */}\n          {step === 'codigo' && (\n            <>\n              {success && (\n                <div className=\"p-4 bg-emerald-900/30 border border-emerald-700 rounded-lg text-emerald-200 text-sm\">\n                  ✓ Código enviado correctamente\n                </div>\n              )}\n\n              <FormInput\n                label=\"Código de Verificación\"\n                type=\"text\"\n                placeholder=\"000000\"\n                maxLength={6}\n                {...form.register('codigoVerificacion')}\n                error={form.formState.errors.codigoVerificacion}\n              />\n\n              <FormInput\n                label=\"Nueva Contraseña\"\n                type=\"password\"\n                placeholder=\"••••••••\"\n                {...form.register('nuevaPassword')}\n                error={form.formState.errors.nuevaPassword}\n              />\n\n              <FormInput\n                label=\"Confirmar Contraseña\"\n                type=\"password\"\n                placeholder=\"••••••••\"\n                {...form.register('confirmarPassword')}\n                error={form.formState.errors.confirmarPassword}\n              />\n\n              {error && (\n                <div className=\"p-4 bg-red-900/30 border border-red-700 rounded-lg text-red-200 text-sm\">\n                  {error}\n                </div>\n              )}\n\n              <button\n                type=\"submit\"\n                disabled={isLoading}\n                className=\"w-full py-3 bg-emerald-600 hover:bg-emerald-700 disabled:opacity-60 disabled:cursor-not-allowed text-white font-semibold rounded-lg transition duration-200\"\n              >\n                {isLoading ? 'Recuperando...' : 'Recuperar Contraseña'}\n              </button>\n            </>\n          )}\n\n          {/* Paso 3: Éxito */}\n          {step === 'nueva-password' && (\n            <>\n              <div className=\"p-4 bg-emerald-900/30 border border-emerald-700 rounded-lg text-emerald-200 text-sm\">\n                ✓ Contraseña recuperada exitosamente\n              </div>\n              <button\n                type=\"button\"\n                onClick={() => navigate('/login')}\n                className=\"w-full py-3 bg-emerald-600 hover:bg-emerald-700 text-white font-semibold rounded-lg transition duration-200\"\n              >\n                Ir a Login\n              </button>\n            </>\n          )}\n\n          {/* Back to Login */}\n          <div className=\"text-center text-sm text-slate-400\">\n            ¿Ya tienes cuenta?{' '}\n            <a href=\"/login\" className=\"text-emerald-500 hover:text-emerald-400\">\n              Inicia sesión aquí\n            </a>\n          </div>\n        </form>\n      </div>\n    </div>\n  );\n}\n